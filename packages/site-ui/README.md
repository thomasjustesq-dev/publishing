# @pub/site-ui

Shared **logic** for the two Astro publications. Brand chrome stays in each site’s
`src/components` and `src/styles` so JAQ and TAS can diverge visually without a
React design-system fork.

Import from `@pub/site-kit` or `@pub/site-ui` (re-exports).
